const divideBy = (firstNumber, secondNumber) => firstNumber / secondNumber;
const addBy = (firstNumber, secondNumber) => firstNumber + secondNumber;

const addBy100 = (num) => addBy(num, 100);
const addBy1000 = (num) => addBy(num, 1000);
const divideBy10 = (num) => divideBy(num, 10);
const divideBy100 = (num) => divideBy(num, 100);